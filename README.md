# Bookmarker

This is a presentational Angular application that uses JSON Server as a lightweight API layer. It implements complete bookmark CRUD operations and a fuzzy search mechanism, all orchestrated through NgRx for state management, action handling, and side-effect control.

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 19.2.6.
